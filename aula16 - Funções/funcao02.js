function soma(n1=0, n2=0) {
  return n1 + n2
}
// toda vez que se quiser limpar alguma coisa na tela, é so substituir a variavel por >>''<< duas aspas simples.
// duas aspas simples é como se fosse vazio, que não é a mesma coisa que null.

console.log(soma('',5))